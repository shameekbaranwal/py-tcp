run: 
	python3 Script.py

setup:
	mkdir FishCongruent && mkdir FishIncongruent && mkdir RobotCongruent && mkdir RobotIncongruent && mkdir FishSingleFeatureInstruction && mkdir FishMultiFeatureInstruction && mkdir RobotSingleFeatureInstruction && mkdir RobotMultiFeatureInstruction
